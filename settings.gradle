pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // This tells app/build.gradle to use its own repo block
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "HyperOS-Gesture-Bypass"
include ':app'